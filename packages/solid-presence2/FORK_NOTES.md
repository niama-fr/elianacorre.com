# @ec/solid-presence2 Fork Notes

This package vendors `solid-presence@0.1.8` plus the minimal `@corvu/utils/reactivity` dependency files it imports.

Current state:

- Source is copied from installed compiled package output and not yet adapted for Solid 2 runtime semantics.
- `@corvu/utils/reactivity` imports were rewritten to local files under `src/corvu`.
