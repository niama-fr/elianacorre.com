import {
  afterPaint,
  callEventHandler,
  combineStyle,
  contains,
  sortByDocumentPosition,
} from './lib'
import type { ElementOf, EventHandlerEvent, Ref } from './types'

export type { EventHandlerEvent, ElementOf, Ref }

export {
  afterPaint,
  callEventHandler,
  combineStyle,
  contains,
  sortByDocumentPosition,
}
